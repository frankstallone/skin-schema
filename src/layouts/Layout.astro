---
import Footer from '../components/Footer.astro';
import Head from '../components/Head.astro';
const { meta, hideFooter = false } = Astro.props;
---

<!doctype html>
<html lang="en">
  <Head {...meta} />
  <body>
    <a href="#main" class="sr-only sr-only-focusable">Skip to content</a>
    <slot />
    {!hideFooter && <Footer />}
  </body>
</html>
